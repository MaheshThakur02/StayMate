<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Darshan Divine</title>
    <link rel="stylesheet" href="/styles.css">
    <script src="/script.js" defer></script>
</head>
<body>
    <!-- Navbar -->
    <!-- <header>
        <nav>
            <div class="logo">
                <img src="/logo.png" alt="Darshan Divine Logo">
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/temples">Temple Bookings</a></li>
                <li><a href="/accommodation">Accommodation</a></li>
                <li><a href="/transport">Transport</a></li>
            </ul>
            <div class="menu-icon">
                <i class="fas fa-bars"></i>
            </div>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-text">
            <h1>Explore the sights of the Temples</h1>
            <p>A place which makes your travel easy</p>
            <div class="search-bar">
                <input type="text" placeholder="Search temple name">
                <input type="text" placeholder="Search destination or city">
                <button>Search</button>
            </div>
        </div>
    </section>

    <!-- Popular Temples -->
    <section class="popular-temples">
        <h2>Popular Temples</h2>
        <div class="temple-cards">
            <% listing.forEach(temple => { %>
                <div class="card">
                    <img src="<%= listing.image %>" alt="<%= listing.title %>">
                    <h3><%= listing.title %></h3>
                    <p><%= listing.description %></p>
                    <button>Book Now</button>
                </div>
            <% }) %>
        </div>
        <button>See all</button>
    </section>

    <!-- Temple Travel Packages -->
    <section class="travel-packages">
        <h2>Temple Travel Packages</h2>
        <div class="package-cards">
            <% packages.forEach(package => { %>
                <div class="card">
                    <img src="<%= package.image %>" alt="<%= package.name %>">
                    <h3><%= package.name %></h3>
                    <p>Starting at <%= package.price %></p>
                    <button>Book Now</button>
                </div>
            <% }) %>
        </div>
        <button>See all</button>
    </section>

    <!-- Best Offers -->
    <section class="best-offers">
        <h2>Best Offer this Month</h2>
        <div class="offer-cards">
            <% offers.forEach(offer => { %>
                <div class="card">
                    <img src="<%= offer.image %>" alt="<%= offer.name %>">
                    <h3><%= offer.discount %> off</h3>
                    <button>Book Now</button>
                </div>
            <% }) %>
        </div>
        <button>Explore All</button>
    </section>

    <!-- Client Reviews -->
    <section class="client-reviews">
        <h2>Clients Review</h2>
        <div class="reviews">
            <% reviews.forEach(review => { %>
                <div class="review-card">
                    <h3><%= review.name %></h3>
                    <p>"<%= review.comment %>"</p>
                </div>
            <% }) %>
        </div>
        <button>See all</button>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-top">
            <div class="value-section">
                <h3>Top values for you</h3>
                <p>Best services to enhance your travel experience</p>
            </div>
            <div class="social-links">
                <a href="#"><i class="fab fa-facebook"></i></a>
                <a href="#"><i class="fab fa-twitter"></i></a>
                <a href="#"><i class="fab fa-instagram"></i></a>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2024 Darshan Divine. All Rights Reserved.</p>
        </div>
    </footer>
</body>
</html> --> -->
